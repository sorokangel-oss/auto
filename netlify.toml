# 이 저장소는 파이썬 프로젝트(main.py, src/ 등)와 정적 페이지(index.html)가
# 함께 있어, Netlify가 별도 빌드 도구(Node/Python)를 자동 감지해 build error를
# 낼 수 있다. command 키를 아예 생략해 빌드 단계 자체를 건너뛰고 저장소 루트를
# 그대로 정적 사이트로 배포한다. (command = "" 처럼 빈 문자열로 두면 Netlify가
# "빈 커맨드"를 실행하려다 실패하는 경우가 있어, 키 자체를 없앤다.)
[build]
  publish = "."
